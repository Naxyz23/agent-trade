# v0.6 — 17 ส.ค. 2026

รอบนี้ไม่ได้เพิ่มฟีเจอร์ใหม่ แต่ไปปิดช่องโหว่ 14 จุดที่เจอจากการตรวจโค้ดทั้ง repo
หลังจากพบว่า brief เช้าวันที่ 17 ส.ค. อ่านข้อมูลเก่า 8.8 ชั่วโมง

## เรื่องเวลา / ข้อมูล

| # | ปัญหา | แก้ยังไง |
|---|-------|----------|
| 1 | Cowork brief วิ่งแซง GitHub Action (เว้นแค่ 20 นาที แต่ cron ของ GitHub ดีเลย์ได้ 100 นาที) | ขยับเวลา brief ให้ห่างจาก workflow 2–3 ชั่วโมง |
| 2 | Yahoo ปล่อยแท่ง Daily ของ crypto ช้า — รัน 01:55 UTC ได้กราฟของ 2 วันก่อน | Kraken เป็นแหล่งหลักของ BTC/ETH/SOL (ปิดแท่ง 00:00 UTC แล้วดึงได้ทันที) Yahoo เป็นตัวสำรอง |

## Risk management ที่หายไปทั้งชุด

| # | ปัญหา | แก้ยังไง |
|---|-------|----------|
| 3 | `halt_drawdown_pct: 15` อยู่ใน config แต่ไม่มีโค้ดไหนอ่านเลย (grep = 0 ครั้ง) | `portfolio.check_drawdown_halt()` เช็คทุกครั้งที่ปิดไม้ |
| 4 | `equity_usdt: 100` เป็นค่าตายตัว ไม่ compounding ตามที่ v5.1 กำหนด | `data/portfolio.json` ติดตามยอดจริง ปรับทุกครั้งที่ปิดไม้ + sync มือได้ |
| 5 | `halts` เป็นแค่ข้อความ — โค้ดยังเปิดไม้ต่อ | `portfolio.entry_blockers()` บล็อกจริง สัญญาณกลายเป็น `kind: "blocked"` |
| 6 | "พัก 3 วัน" ไม่มีการนับวัน และไม่รีเซ็ต streak → halt ค้างตลอดกาล = deadlock | เก็บ `paused_until` เป็นวันที่ + รีเซ็ต `consecutive_losses` ทันทีที่สั่งพัก |
| 7 | `pct_per_trade` ใน yml ไม่ถูกอ่าน (rules.py hardcode ไว้) | ส่งค่าจาก config เข้า `rules.evaluate(risk_pct=...)` |

## state ไม่ตรงกับพอร์ตจริง

| # | ปัญหา | แก้ยังไง |
|---|-------|----------|
| 8 | ระบบเปิดไม้ให้เองทุกครั้งที่มีสัญญาณ ทั้งที่ประกาศว่า "ไม่ auto-trade" — สถิติและ streak มาจากไม้ที่อาจไม่เคยเกิดขึ้น | สัญญาณไปอยู่ `pending` รอ Nana ยืนยันผ่าน `data/manual.json` · ไม่ยืนยันใน 2 วันหมดอายุ |
| 9 | exit เช็ค SL ด้วยราคาปิด — ไส้หลุด SL แล้วเด้งกลับ ระบบบอกว่ายังถืออยู่ ทั้งที่กระดานตัดไปแล้ว | เช็คด้วย `low`/`high` |
| 10 | P&L ตอนชน SL คิดจากราคาปิด ทำให้ตัวเลขขาดทุนผิดและเพี้ยนต่อที่ streak | ออกที่ราคา `stop` จริง · ถ้าเปิด gap ข้าม SL ใช้ราคาเปิด (`reason: stop_gap`) |

## โครงสร้าง

| # | ปัญหา | แก้ยังไง |
|---|-------|----------|
| 11 | `gold.yml` กับ `stock.yml` ตั้ง cron ชนกันเป๊ะ แต่คนละ concurrency group → git push ชนกัน ผลรอบนั้นหายเงียบ | เว้นเวลา 30 นาที + `concurrency: agent-trade-data` ร่วมกัน + retry loop 5 รอบ |
| 12 | `data/signals.json` + `data/state.json` ค้างจาก v0.4 ไม่มีอะไรเขียนทับ | workflow สั่ง `git rm` ทิ้งอัตโนมัติ |
| 13 | engine ไม่เช็คความสดของกราฟเลย ข้อมูลเก่า 5 วันก็รันเงียบ ๆ | `data_health` ต่อ symbol · เก่าเกินเกณฑ์ = ไม่ออกสัญญาณ + ขึ้น halt |
| 14 | `brief_*.html` เป็น artifact 14 วัน Cowork เปิดดูไม่ได้ | commit เข้า repo ด้วย |

## ของแถม

- test runner เดิมจับแค่ `AssertionError` — ถ้า test พังด้วย exception อื่น ทั้ง suite จะตายกลางคัน
- เพิ่ม test อีก 25 ตัว (รวม 51) ครอบทุกข้อข้างบน — ของเดิมไม่มี test ของ halt/pause/drawdown/state เลย

## ไฟล์ใหม่

- `engine/portfolio.py` — ยอดพอร์ต, drawdown, พัก, pending, การยืนยันด้วยมือ
- `data/portfolio.json` — ยอดพอร์ตก้อนเดียวใช้ร่วมกันทั้ง 3 agent
- `data/manual.json` — ช่องทางให้ Nana บอกระบบว่าเกิดอะไรจริง (แก้จากเว็บ GitHub ได้)

## สิ่งที่ยังไม่ได้ทำ (หนี้ทางเทคนิค)

- หุ้น US 13 ตัวยังไม่เคยผ่าน backtest ของ v5.1 (proven เฉพาะ BTC/ETH/BNB/PAXG/NVDA)
- XAU ยังไม่เคย backtest ตรงตัว (v5.1 ใช้ PAXG เป็น proxy)
- ATR x2 stop ดูดีบน BTC (บวก 14/15 ปี, maxDD ต่ำสุด) แต่ยังไม่ได้ทดสอบข้ามสินทรัพย์
