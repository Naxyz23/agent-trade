# engine v0.9.2 — 2026-08-22 · ย่อข้อความแจ้งเตือน Discord

**กฎการเทรดไม่เปลี่ยน** — spec ยังเป็น `cngoal-6.1` เหมือนเดิม แก้เฉพาะรูปแบบข้อความ

## ปัญหา
Nana: "การส่งสัญญาณแต่ละครั้งยาวมาก เอาแค่มีตัวใดผ่านเงื่อนไขกี่ข้อก็พอ"

ของเดิม `notify.py` ส่ง **1 embed ต่อ 1 สัญญาณ** แต่ละอันมี reasons 6 บรรทัด + fields 6 ช่อง
+ footer แยก → วันที่มี 2 สัญญาณก็ต้องเลื่อนอ่านทั้งจอ ทั้งที่เนื้อหา 90% ซ้ำกับ `brief.html`

## แก้เป็น
**1 ข้อความ = 1 embed · 1 สินทรัพย์ = 1 บรรทัด**

```
[US Stock] **Signal Brief 2026-08-19** — เข้า 1 · ออก 0 · บล็อก 1
🟢 **ORCL** SHORT · 4/4 · E 142.79 · SL 157.14 (10.1%) · size 9.85
🟠 **JPM** LONG · 4/4 · บล็อก: มีไม้เปิดอยู่ 1 ไม้ (เพดาน 1)
👀 ใกล้ครบ: NVDA 3/4 · MSFT 3/4 · META 3/4 · TSLA 3/4 · LLY 3/4
CNgoal v6.1 · แท่ง 2026-08-18 · รายละเอียดเต็มใน brief.html
```

- `passed()` นับ "ผ่านกี่ข้อ" จาก `len(checklist)` จริง — crypto 5 ข้อ · stock/commodity 6 ข้อ
  ⛔ ห้าม hardcode "4 ข้อ" อีก (ของเดิมค้างมาตั้งแต่ v5.1)
- exit: `🔴 SYM SHORT · 142.79 → 150.10 · -5.12% · <เหตุผลออก>`
- watch ทั้งหมดรวบเป็นบรรทัดเดียว เรียงตัวที่ใกล้ครบขึ้นก่อน เกิน 8 ตัวตัดเป็น `+N`
- halt ยังขึ้นบรรทัดแรกและติด 🛑 ที่หัวข้อความเหมือนเดิม
- สีของ embed = เรื่องที่ด่วนที่สุดในรอบนั้น (halt/exit แดง → entry เขียว → blocked ส้ม)

## แถม: ความละเอียดราคา
`_fmt()` เดิมใช้ 4 significant digits ทำให้ **142.79 กลายเป็น 142.8** (เอาไปตั้งออเดอร์ไม่ตรง)
เปลี่ยนเป็น: เลข ≥ 1 → ทศนิยม 2 ตำแหน่ง · เลขเล็กกว่า 1 (เหรียญราคาต่ำ) → 6 significant digits

## เทสต์ 74 → 77
- `test_notify_message_is_compact` — 6 entry + 14 watch ต้องได้ 7 บรรทัด · ไม่มี fields · < 1200 ตัวอักษร
- `test_notify_passed_counts_are_not_hardcoded` — 4/5 กับ 6/6 ต้องถูกทั้งคู่
- `test_notify_price_precision_is_order_ready` — 142.79 ต้องไม่ถูกปัด
