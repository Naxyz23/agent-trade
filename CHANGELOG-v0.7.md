# v0.6 + v0.7 — 17 ส.ค. 2026

ตรวจโค้ดทั้ง repo หลังพบว่า brief เช้าวันที่ 17 ส.ค. อ่านข้อมูลเก่า 8.8 ชั่วโมง
เจอช่องโหว่ 14 จุด แก้ครบใน v0.6 · v0.7 ปรับเรื่องการบันทึกไม้ตามที่ Nana เลือก

**test 56/56 ผ่าน** (ของเดิม 26) + smoke test แบบเดินเวลา 12 วันครบวงจร

---

## v0.7 — โหมด auto (สรุปสั้น)

v0.6 เคยให้สัญญาณ entry ไปรอยืนยันใน `data/manual.json` ก่อน — ถูกต้องในแง่บัญชี
แต่ต้องแก้ไฟล์ทุกครั้งที่เข้า/ออกไม้ซึ่งยุ่งเกินไป จึงกลับมาเป็น **auto**:
มีสัญญาณ + ผ่าน risk gate = นับเป็นไม้ทันที

`manual.json` เหลือแค่ 2 ช่องที่ยังต้องใช้คนตัดสินใจจริง ๆ:

| ช่อง | เมื่อไหร่ที่ใช้ |
|---|---|
| `equity` | ยอดที่ระบบคำนวณเริ่มห่างจากยอดจริงบนกระดาน (fee จริง, funding, เข้าไม้คนละราคา) |
| `resume` | ปลดล็อกหลังโดน halt จาก drawdown 15% — v5.1 บอกให้ "ทบทวนกฎ" ถ้าปลดเองอัตโนมัติกฎข้อนี้ก็ไร้ความหมาย |

**auto ของ v0.7 ต่างจาก auto ของ v0.5 ตรงไหน** — เดิมมันเปิดไม้โดยไม่มีอะไรห้ามเลย ตอนนี้:

- ต้องผ่าน `entry_blockers()` ก่อน (halt / ช่วงพัก / เพดานไม้ บล็อกได้จริง)
- `size` คิดจาก equity ปัจจุบันที่เดินตามผลเทรดจริง ไม่ใช่ 100 ตายตัวตลอดกาล
- ตอนปิดเช็ค SL ด้วยไส้เทียนและออกที่ราคา stop จริง

**ข้อจำกัดที่ต้องรู้:** ราคาที่บันทึกคือราคาปิดของแท่งที่ให้สัญญาณ ของจริงได้ราคาเปิดแท่งถัดไป
ถ้าอยากให้ตรงเป๊ะ ทางเดียวคือต่อ Binance read-only API มาอ่านไม้จริง (ยังไม่ได้ทำ)

---

## เรื่องเวลา / ข้อมูล

| # | ปัญหา | แก้ยังไง |
|---|-------|----------|
| 1 | Cowork brief วิ่งแซง GitHub Action (เว้นแค่ 20 นาที แต่ cron ของ GitHub ดีเลย์ได้ 100 นาที) | ขยับ brief ให้ห่าง 2–3 ชั่วโมง: crypto 10:00 · gold 15:30 · stock 16:00 น. |
| 2 | Yahoo ปล่อยแท่ง Daily ของ crypto ช้า — รัน 01:55 UTC ได้กราฟของ 2 วันก่อน | Kraken เป็นแหล่งหลักของ BTC/ETH/SOL (ปิดแท่ง 00:00 UTC แล้วดึงได้ทันที) Yahoo เป็นสำรอง |

## Risk management ที่หายไปทั้งชุด

| # | ปัญหา | แก้ยังไง |
|---|-------|----------|
| 3 | `halt_drawdown_pct: 15` อยู่ใน config แต่ไม่มีโค้ดไหนอ่านเลย (grep = 0 ครั้ง) | `portfolio.check_drawdown_halt()` เช็คทุกครั้งที่ปิดไม้ |
| 4 | `equity_usdt: 100` เป็นค่าตายตัว ไม่ compounding ตามที่ v5.1 กำหนด | `data/portfolio.json` ติดตามยอดจริง ปรับทุกครั้งที่ปิดไม้ |
| 5 | `halts` เป็นแค่ข้อความ — โค้ดยังเปิดไม้ต่อ | `entry_blockers()` บล็อกจริง สัญญาณกลายเป็น `kind: "blocked"` |
| 6 | "พัก 3 วัน" ไม่มีการนับวัน และไม่รีเซ็ต streak → halt ค้างตลอดกาล = deadlock | เก็บ `paused_until` เป็นวันที่ + รีเซ็ต `consecutive_losses` ทันทีที่สั่งพัก |
| 7 | `pct_per_trade` ใน yml ไม่ถูกอ่าน (rules.py hardcode ไว้) | ส่งค่าจาก config เข้า `rules.evaluate(risk_pct=...)` |

## state ไม่ตรงกับพอร์ตจริง

| # | ปัญหา | แก้ยังไง |
|---|-------|----------|
| 8 | ระบบเปิดไม้โดยไม่มี risk gate ใด ๆ ขวาง | v0.7 ยังเปิดอัตโนมัติ แต่ต้องผ่าน `entry_blockers()` ก่อนเสมอ |
| 9 | exit เช็ค SL ด้วยราคาปิด — ไส้หลุด SL แล้วเด้งกลับ ระบบบอกว่ายังถืออยู่ ทั้งที่กระดานตัดไปแล้ว | เช็คด้วย `low`/`high` |
| 10 | P&L ตอนชน SL คิดจากราคาปิด ทำให้ตัวเลขขาดทุนผิดและเพี้ยนต่อที่ streak | ออกที่ราคา `stop` จริง · เปิด gap ข้าม SL ใช้ราคาเปิด (`reason: stop_gap`) |

## โครงสร้าง

| # | ปัญหา | แก้ยังไง |
|---|-------|----------|
| 11 | `gold.yml` กับ `stock.yml` ตั้ง cron ชนกันเป๊ะ แต่คนละ concurrency group → git push ชนกัน ผลรอบนั้นหายเงียบ | เว้นเวลา 30 นาที + `concurrency: agent-trade-data` ร่วมกัน + retry loop 5 รอบ |
| 12 | `data/signals.json` + `data/state.json` ค้างจาก v0.4 ไม่มีอะไรเขียนทับ | workflow สั่ง `git rm` ทิ้งอัตโนมัติ |
| 13 | engine ไม่เช็คความสดของกราฟเลย ข้อมูลเก่า 5 วันก็รันเงียบ ๆ | `data_health` ต่อ symbol · เก่าเกินเกณฑ์ = ไม่ออกสัญญาณ + ขึ้น halt |
| 14 | `brief_*.html` เป็น artifact 14 วัน Cowork เปิดดูไม่ได้ | commit เข้า repo ด้วย |

## 2 บั๊กที่เจอเพิ่มระหว่างทดสอบ (ไม่ได้อยู่ใน 14 ข้อแรก)

**ก. ปิดไม้แล้วไม่ถูกเอาออกจากทะเบียน** — ไม้เดิมถูก "ปิด" ซ้ำทุกวัน ยอดพอร์ตไหลลงเรื่อย ๆ
และ `consecutive_losses` พุ่งจนสั่งพักทั้งที่มีไม้เดียว
เจอตอน smoke test เดินเวลา 12 วัน (ทดสอบแบบยิงครั้งเดียวมองไม่เห็น)
→ ย้ายการลบไม้เข้าไปอยู่ใน `close_position()` จุดเดียว ผู้เรียกลืมไม่ได้อีก

**ข. ไม่มีตัวกันข้อมูลเพี้ยน** — ไม้ที่ตั้ง SL ไว้ไม่ควรขาดทุนเกิน ~1R
ถ้าคำนวณออกมาว่าขาดทุนเกิน 5R แปลว่าเกือบแน่นอนว่าข้อมูลผิด ไม่ใช่ตลาดพังจริง:
หุ้นแตกพาร์แล้วราคาย้อนหลังถูกปรับ · fallback สลับแหล่ง (Kraken ↔ Yahoo) สเกลไม่ตรง · feed คืนค่าขยะ
ถ้าปล่อยผ่าน ยอดพอร์ตจะพังถาวรและลาก drawdown halt มาด้วย
→ บันทึกผลที่ราคา stop ตามแผนแทน (คือสิ่งที่ SL order จริงจะทำ) เก็บตัวเลขดิบไว้ใน
`closed[].raw_exit` ติดธง `suspicious_data` และเตือนให้ไปเช็คยอดจริง
ปรับเกณฑ์ได้ที่ `risk.max_gap_loss_r`

## ของแถม

- test runner เดิมจับแค่ `AssertionError` — ถ้า test พังด้วย exception อื่น ทั้ง suite จะตายกลางคัน
- test เพิ่มจาก 26 → 56 ตัว ครอบทุกข้อข้างบน (ของเดิมไม่มี test ของ halt/pause/drawdown/state เลย)

## ไฟล์ใหม่

- `engine/portfolio.py` — ยอดพอร์ต, drawdown, พัก, เปิด/ปิดไม้, ตัวกันข้อมูลเพี้ยน
- `data/portfolio.json` — ยอดพอร์ตก้อนเดียวใช้ร่วมกันทั้ง 3 agent
- `data/manual.json` — 2 ช่อง: sync ยอดจริง / ปลดล็อก halt

## สิ่งที่ยังไม่ได้ทำ

- **ต่อ Binance read-only API** — Nana เทรดทุกอย่างบน Binance ถ้าต่อ `/fapi/v3/positionRisk`
  กับ `/fapi/v2/balance` ระบบจะรู้ไม้จริงและยอดจริงเอง แม่นกว่า auto ที่เดาจากราคาปิด
  และไม่ต้อง sync equity ด้วยมืออีกเลย (key แบบ read-only เทรด/ถอนเงินไม่ได้)
- หุ้น US 13 ตัวยังไม่เคยผ่าน backtest ของ v5.1 (proven เฉพาะ BTC/ETH/BNB/PAXG/NVDA)
- XAU ยังไม่เคย backtest ตรงตัว (v5.1 ใช้ PAXG เป็น proxy)
- ATR x2 stop ดูดีบน BTC (บวก 14/15 ปี, maxDD ต่ำสุด) แต่ยังไม่ได้ทดสอบข้ามสินทรัพย์
- `README.md` / `SETUP.html` / `design.html` ยังเป็นเนื้อหา v0.5
