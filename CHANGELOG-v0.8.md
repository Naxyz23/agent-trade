# v0.8 — ปรับ exit และ stop ตามผล backtest (18 ส.ค. 2026)

เวอร์ชันนี้**ไม่แตะเงื่อนไขเข้าไม้เลย** — checklist 4 ข้อของ CNgoal v5.1 ยังเหมือนเดิมทุกประการ
เปลี่ยนแค่ 2 อย่าง: เส้นที่ใช้ trail ออก และวิธีวาง SL

หลักฐาน: `backtest-report-v2.html`
(หุ้น US 13 ตัว + XAU + SPY = 3,581 ไม้ ตลอด 53 ปี · BTC 5,343 แท่ง 2012-2026)
เข้าไม้ใน backtest เรียก `engine/rules.py` ตัวจริง ไม่ได้เขียนกฎขึ้นใหม่

---

## 1. Exit: trail EMA20 → **EMA50** (ทุกสินทรัพย์)

**ทำไม** — กำไรทั้งหมดของระบบมาจากหางยาว: ไม้ที่ได้เกิน 3R มีแค่ **6.9%**
แต่ทำได้ **+1,030R** ขณะที่อีก 93% รวมกันติดลบ −581R
EMA20 ตัดกำไรเร็วเกินไปจึงฆ่าหางยาวนั้นทิ้ง

| | หุ้น + ทอง | BTC |
|---|---|---|
| trail EMA20 (เดิม) | +0.053R | +1.947R |
| trail EMA50 (ใหม่) | **+0.157R** | **+2.877R** |
| ไม้ที่ได้ >3R | 5.6% → 7.9% | — |

ดีขึ้นทั้งสองตลาดที่ไม่เกี่ยวข้องกันเลย = หลักฐานหนักแน่นกว่าผลจากกลุ่มเดียว

**ผลข้างเคียงที่ต้องรู้:** `reason` ของ exit เปลี่ยนจาก `ema20_cross` เป็น `ema50_cross`
และจำนวนไม้ลดลงราว 28% (ถือไม้นานขึ้น)

## 2. Stop: เพิ่มโหมด **ATR14 × 2** ให้หุ้น US และ XAU (crypto คงเดิม)

**ทำไมต้องแยกตามสินทรัพย์ ห้ามใช้อันเดียวทั้งระบบ**

| | SL ตามชาร์ต | SL = ATR × 2 |
|---|---|---|
| หุ้น + ทอง | +0.157R | **+0.178R** |
| หุ้น + ทอง ที่ slippage 0.40% | **−0.205R (พัง)** | **+0.004R (ยังเสมอตัว)** |
| BTC | **+2.877R** | +2.084R |

ประโยชน์ที่ใหญ่ที่สุดของ ATR × 2 ไม่ใช่ตัวเลขกำไร แต่คือ **ความทนต่อต้นทุน** —
กฎเดิมติดลบตั้งแต่ slippage 0.10% ส่วนกฎใหม่ยังเสมอตัวที่ 0.40% (ทนได้ราว 4 เท่า)

ส่วน BTC ไม่ได้ประโยชน์ ตรงกับผลทดสอบ SL เมื่อ 17 ส.ค. ที่สรุปว่า
SL ตามชาร์ตทนทานที่สุดสำหรับ crypto → **crypto ไม่เปลี่ยน**

---

## ผ่านครบ 5 ด่านก่อนนำมาใช้

| ด่าน | ผล |
|---|---|
| out-of-sample (ตั้งแต่ 2019 กันไว้ตั้งแต่ต้น) | +0.130R (คร่อมศูนย์) → **+0.353R** [+0.143, +0.584] |
| ทนต่อ slippage | พังที่ 0.10% → **ยังเสมอตัวที่ 0.40%** |
| กระจายตัว | **ดีขึ้น 12 จาก 15 สินทรัพย์** (แย่ลงตัวเดียวคือ LLY) |
| ความคงเส้นคงวาตามยุค | 2000s −0.09 · 2010s −0.03 → **+0.03 · +0.09** (บวกทุกยุคยกเว้น 1970s) |
| ตลาดอื่น | BTC ก็ดีขึ้น (คนละ asset class คนละแหล่งข้อมูล) |

## สิ่งที่ทดสอบแล้ว "ไม่ผ่าน" — อย่าเสนอซ้ำ

| ไอเดีย | ผล | สรุป |
|---|---|---|
| เลื่อน SL มาจุดทุนเมื่อกำไรถึง 1R | +0.022R (เดิม +0.053R) | **แย่กว่าเดิม** — ไม้ที่จะวิ่งไป 10R มักย่อกลับมาแตะจุดทุนก่อน |
| ชอร์ตเฉพาะตอน SPY EMA200 หันลงด้วย | −0.257R (เดิม −0.203R) | แย่ลง |
| ชอร์ตเฉพาะตอนราคาอยู่ใต้ EMA200 | −0.201R | ไม่เปลี่ยน (1,025/1,031 ไม้เข้าเงื่อนไขอยู่แล้ว) |
| ชอร์ตเฉพาะตอน RSI14 < 40 | −0.141R | ดีขึ้นบ้าง แต่ยังติดลบ |
| **ปิดการชอร์ตบนหุ้น/ทอง** | — | **ข้อเสนอที่ผิด ถอนแล้ว** (ดูด้านล่าง) |

### ทำไมไม่ปิดการชอร์ต

ตัวเลข short −0.203R บนหุ้นเกิดจาก **กลุ่มทดสอบลำเอียง** ไม่ใช่กฎพัง —
หุ้น 13 ตัวที่เลือกมาโตปีละ 21.7% ขณะที่ตลาดรวมโต 8.9% (ลำเอียง +12.7%/ปี)

แบ่งไม้ชอร์ตตามสิ่งที่ราคาทำจริงใน 60 วันหลังเข้าไม้:

| ราคาหลังเข้าไม้ | ไม้ | ชนะ | expectancy |
|---|---|---|---|
| **ร่วง >15%** | 131 | 46.6% | **+0.935R** [+0.471, +1.464] |
| ขึ้น (ทุกระดับ) | 652 | 13–22% | −0.36 ถึง −0.55R |

**กฎชอร์ตทำงานได้ดีเมื่อราคาลงจริง** และบน BTC ที่มีขาลงจริง ชอร์ตให้ **+0.664R**
ในการเทรดจริงไปข้างหน้าไม่มี survivorship bias → **เก็บชอร์ตไว้ทุกสาย**

---

## ตัวเลือกที่ยังไม่ได้ใส่ (ถ้าอยากได้อัตราชนะสูงเป็นหลัก)

"เริ่ม trail EMA20 หลังกำไรถึง 1R" (ก่อนหน้านั้นออกเฉพาะชน SL) ให้:

| | อัตราชนะ | expectancy |
|---|---|---|
| กฎเดิม | 30.6% | +0.073R |
| EMA50 (ที่เลือกใช้) | 28.1% | **+0.178R** |
| EMA20 หลังกำไร 1R | **41.8%** | +0.124R |

เลือก EMA50 เพราะให้กำไรรวมสูงสุด (+463R เทียบ +357R)
ถ้าเปลี่ยนใจอยากได้ชนะบ่อยกว่า ต้องเพิ่มโหมด exit ใหม่ใน `cngoal_exit`

---

## ไฟล์ที่แก้

| ไฟล์ | แก้อะไร |
|---|---|
| `engine/rules.py` | เพิ่ม `_place_stop()` · `cngoal_entry(stop_mode=)` · `cngoal_exit(trail_ema=)` · `evaluate()` ส่งค่าต่อ |
| `engine/run.py` | อ่าน `rules:` จาก watchlist · resolve stop_mode รายตัว > ตาม class > ค่าเริ่มต้น · engine_version 0.8 · ใส่ `rules_config` ลง signals json |
| `watchlist.yml` | บล็อก `rules:` ใหม่ พร้อมเหตุผลและตัวเลขกำกับ |
| `tests/test_engine.py` | 63 เทสต์ (เดิม 56) |
| `cowork/SKILL.md` | reason code ใหม่ + `stop_source` |

## บั๊กที่เจอระหว่างทำ v0.8

1. **เทสต์ exit เดิมถูกข้ามเงียบ ๆ** — ครอบด้วย `if r["close"] < r["ema20"]` ซึ่งเป็นจริง
   บ้างไม่จริงบ้างตาม seed พอเปลี่ยนค่าเริ่มต้นเป็น EMA50 เทสต์ก็ยัง PASS
   ทั้งที่ไม่ได้ตรวจอะไรเลย → เขียนใหม่ให้วนหา seed ที่เข้าเงื่อนไขจริง
   และ **บังคับว่าต้องเจออย่างน้อย 1 เคส** ไม่งั้นถือว่าเทสต์ล้มเหลว
2. **smoke test รอบแรกหลอกตัวเอง** — สร้าง position dict ขึ้นเองแทนที่จะเรียก
   `portfolio.open_position()` ทำให้ขาด `size`/`risk_amount` แล้ว **equity ไม่ขยับเลย
   ทั้งที่ปิดไม้ไป 21 ครั้ง** แต่ assert ทุกข้อยังผ่าน → เพิ่ม assert ว่า equity
   ต้องเปลี่ยนจริง และต้องเรียกผ่าน path เดียวกับ `run.py`
3. `run.py` ยังเขียนข้อความว่า "จะติดตาม SL/EMA20 ให้อัตโนมัติ" → เปลี่ยนเป็นตัวแปร

## Smoke test เดินเวลาจริง (1,140 วัน × 3 สินทรัพย์)

| | เปิด/ปิด/ค้าง | equity | reasons |
|---|---|---|---|
| NVDA (atr2) | 21 / 21 / 0 | 100 → 114.85 | ema50_cross 15 · stop 5 · stop_gap 1 |
| XAU (atr2) | 17 / 17 / 0 | 100 → 129.13 | ema50_cross 13 · stop 4 |
| BTC (chart) | 27 / 27 / 0 | 100 → 135.45 | stop 15 · ema50_cross 12 |

ไม่มีไม้ค้าง ไม่มีการปิดซ้ำ stop_mode ถูกเลือกตาม class ถูกต้อง
