# .github/workflows/gold.yml
name: gold-signals

on:
  schedule:
    - cron: "10 6 * * 2-6"
  workflow_dispatch: {}

permissions:
  contents: write

concurrency:
  group: gold-signals
  cancel-in-progress: false

jobs:
  scan:
    runs-on: ubuntu-latest
    env:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -r requirements.txt
      - name: Self-test
        run: python tests/test_engine.py
      - name: Run scanner (gold)
        run: python -m engine.run --group gold
      - name: Commit results
        run: |
          git config user.name  "signal-bot"
          git config user.email "bot@users.noreply.github.com"
          git add data/signals_gold.json data/state_gold.json
          git diff --staged --quiet || git commit -m "gold signals $(date -u +%F_%H:%M)"
          git pull --rebase --autostash || true
          git push
      - name: Notify Discord
        if: ${{ env.DISCORD_WEBHOOK != '' }}
        run: python -m engine.notify --group gold
      - name: Upload brief
        if: always()
        run: python -m engine.render data/signals_gold.json data/brief_gold.html
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: brief-gold
          path: data/brief_gold.html
          retention-days: 14
